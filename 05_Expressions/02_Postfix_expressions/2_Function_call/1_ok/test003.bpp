#include <stdio.h>

int f()
{
  printf("f() called\n");
}

int f(int a)
{
  printf("f(int) called\n");
  printf("a = %d\n", a);
}

int f(int a, int b)
{
  printf("f(int, int) called\n");
  printf("a = %d, b = %d\n", a, b);
}

int main()
{
  f();
  f(1);
  f(2,3);
  return 0;
}
