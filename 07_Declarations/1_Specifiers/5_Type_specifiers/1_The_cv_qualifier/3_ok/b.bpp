#include <stdio.h>

const int sz = 7;

int array[sz];

void g()
{
  for ( int i = 0 ; i < sz ; ++i )
    array[i] = i;
}

void h()
{
  for ( int i = 0 ; i < sz ; ++i )
    printf("array[%d] = %d\n", i, array[i]);
}
