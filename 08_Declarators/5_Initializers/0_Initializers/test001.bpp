#include <stdio.h>

int b[] = { 0, 1, 2, 3, 4 };

int a = b[3];

int main(void)
{
  for ( int i = 0 ; i < sizeof b/sizeof b[0] ; ++i )
    printf("b[%d] = %d\n", i, b[i]);
  printf("a = %d\n", a);
  return 0;
}
